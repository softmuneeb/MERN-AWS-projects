import { nftBuyBot } from './nftBot.js';

const startAccId = 52,
  endAccId = 55,
  waitInSecondsFrom = 1,
  waitInSecondsTo = 3;

nftBuyBot(startAccId, endAccId, waitInSecondsFrom, waitInSecondsTo);
