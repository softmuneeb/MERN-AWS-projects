// what this code do? 

// plan
//

const init = async () => {
  // What is the output of the following code?

let arr = [1, "Turing", {x : 2}, [3, 4]];

delete arr[1];

console.log(arr.length);
};

init();
